language: objective-c
osx_image: xcode11.2

env:
  global:
    - LC_CTYPE=en_US.UTF-8
    - LANG=en_US.UTF-8

notifications:
  email: false

cache: cocoapods

before_install: 
    - gem install cocoapods 
    - pod --version

script:
  - pwd
  - ls
#  - pod trunk register changsanjiang@gmail.com changsanjiang
#  - pod trunk me
#  - pod trunk push --allow-warnings --use-libraries
